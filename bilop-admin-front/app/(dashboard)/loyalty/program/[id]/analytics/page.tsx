
const AnalyticsPage = async ({ params }: { params: { id: string } }) => {

  //const analytics = await getProgramAnalytics(params.id)

 // return <AnalyticsProgram program={program} analytics={analytics} />
}

export default AnalyticsPage;
